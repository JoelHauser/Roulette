bin/
obj/
*.user
*.suo
.vs/
.idea/
TestResults/
dist/

# The harness publishes self-contained, so its zip is thirty megabytes and rebuilds
# from source in seconds. The SPT mod zip is small and gets tracked the way
# Blackjack's and Poker's are.
releases/Roulette.Console-*.zip
